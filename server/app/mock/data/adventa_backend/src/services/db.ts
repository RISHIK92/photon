// [MOCK DATA] Prisma client singleton — condensed stub, real repo has
// connection pooling config here too.
import { PrismaClient } from "@prisma/client";

export const prisma = new PrismaClient();
