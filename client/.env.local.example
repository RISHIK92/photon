NEXT_PUBLIC_BRAIN_API_URL=http://localhost:8000
NEXT_PUBLIC_LIVEKIT_URL=

# Server-side only (the token-minting route needs these) — never sent to
# the browser, so this is safe to keep in .env.local rather than a
# NEXT_PUBLIC_ variable.
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
